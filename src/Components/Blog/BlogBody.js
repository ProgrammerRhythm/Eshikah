import React from 'react';
import { useParams } from 'react-router-dom';

const BlogBody = () => {
    const {id} = useParams;
    return (
        <div>
            
        </div>
    );
};

export default BlogBody;